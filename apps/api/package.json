{
  "name": "@multiproject-rag/api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --port 8000",
    "lint": "uv run ruff check app tests",
    "format": "uv run ruff format app tests",
    "test": "uv run pytest"
  }
}
