{
  "name": "multiproject-rag",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:api": "pnpm --filter @multiproject-rag/api dev",
    "dev:public": "pnpm --filter @multiproject-rag/public-web dev",
    "dev:admin": "pnpm --filter @multiproject-rag/admin-web dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "turbo format"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.9.10",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
